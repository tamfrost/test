name: 'Asciidoctor'
description: 'Build dockumentation with asciidoctor'

inputs:
  function:
    description: 'function to run'
    required: true

runs:
  using: 'docker'
  image: 'asciidoctor/docker-asciidoctor'
  entrypoint: /bin/bash
  args:
    - -c
    - |
      ls -al
      ls -al .scripts/cicd/jobs.sh
      source .scripts/cicd/jobs.sh
      ${{ inputs.function }}